name: transport_logistics
description: Transport & Logistics production-ready starter
publish_to: "none"

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.0
  cloud_firestore: ^4.9.0
  firebase_auth: ^4.7.0
  firebase_messaging: ^14.5.0
  provider: ^6.0.6
  google_maps_flutter: ^2.4.7
  flutter_dotenv: ^6.0.1
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
